syntax = "proto3";

package grpc;

service Chat {
    rpc SendMsg (Msg) returns (Empty);
    rpc Channel (Empty) returns (stream Msg);
}

message Empty {}

message Msg {
    string id = 1;
    string client_id = 2;
    string dest_id = 3;
    string message = 4;
} 
